name: autotester_ai
description: AutoTester AI — Mobile Agent Engine
publish_to: none
version: 3.0.0+1

environment:
  sdk: ^3.10.4

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.1
  flutter_dotenv: ^5.2.1
  shared_preferences: ^2.2.2
  share_plus: ^10.0.0
  path_provider: ^2.1.2
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - .env